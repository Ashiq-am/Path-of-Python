# Find the length of the newly
# created list
print(len(Row_list))

# Print the first 3 elements
print(Row_list[:3])




"""
we have successfully extracted each row of the given 
dataframe into a list. Just like any other Python’s list we can perform 
any list operation on the extracted list

"""
