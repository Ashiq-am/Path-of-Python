# import the module
from vpython import *

# the first cylinder
cylinder(pos = vector(-2, 2, 0),
		length = 3,
		radius = 1,
		color = vector(0.5, 0, 0))

# the second cylinder
cylinder(pos = vector(1, -1, 5),
		color = vector(0, 1, 0))
